# Learn folder

Standalone exercises — not wired to the main app.

- **js-from-scratch/** — small HTML pages for DOM, events, fetch, try/catch. Open each file directly in the browser.

The live app lives in `frontend/`.
