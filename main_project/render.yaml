services:
  - type: web
    name: ai-learning-job-assistant
    runtime: docker
    plan: free
    healthCheckPath: /health
    disk:
      name: notes-data
      mountPath: /app/data
      sizeGB: 1
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
